# Локализация проекта Sound Level Meter

## 📋 Поддерживаемые языки

Приложение поддерживает **7 языков**:
- 🇬🇧 **English (en)** - базовый язык
- 🇪🇸 **Español (es)** - испанский
- 🇮🇹 **Italiano (it)** - итальянский
- 🇫🇷 **Français (fr)** - французский
- 🇩🇪 **Deutsch (de)** - немецкий
- 🇧🇷 **Português (pt-BR)** - португальский (Бразилия)
- 🇷🇺 **Русский (ru)** - русский

---

## 📁 Структура файлов

```
Sound Level Meter/
└── Resources/
    └── Localizations/
        ├── Localizable.xcstrings    # String Catalog (все переводы)
        └── LOCALIZATION_GUIDE.md    # Эта инструкция
Core/
└── Localization/
    └── LocalizedString.swift        # Swift API для доступа к переводам
```

---

## 🎯 Как добавить новый текст для перевода

### Вариант 1: Через Xcode (рекомендуется)

1. Откройте `Localizable.xcstrings` в Xcode
2. Нажмите `+` внизу окна
3. Введите ключ (например: `button.new_feature`)
4. Xcode автоматически покажет все языки для перевода
5. Заполните переводы для каждого языка

### Вариант 2: Через код (программисты)

1. Откройте `LocalizedString.swift`
2. Добавьте новый ключ в соответствующий enum:

```swift
enum LocalizedString {
    enum MyFeature {
        static let title = String(localized: "my_feature.title",
                                  comment: "My feature title")
    }
}
```

3. Откройте `Localizable.xcstrings`
4. Добавьте переводы для ключа `my_feature.title`

---

## 💡 Использование в коде

### SwiftUI Views
```swift
import SwiftUI

struct MyView: View {
    var body: some View {
        VStack {
            Text(LocalizedString.History.title)
            Button(LocalizedString.Common.done) { }
        }
    }
}
```

### UIKit / Обычный Swift
```swift
let title = LocalizedString.History.title
print(title) // "History" / "Historial" / "Cronologia" и т.д.
```

---

## 🔑 Соглашения по именованию ключей

Используйте структуру: `категория.подкатегория.название`

### Примеры:
```
✅ Хорошо:
- common.cancel
- history.empty.title
- settings.audio.weighting
- permission.microphone.message

❌ Плохо:
- cancelButton
- HistoryEmpty
- AudioWeightingInSettings
```

### Категории:
- `common.*` - общие элементы (кнопки, действия)
- `main.*` - главный экран
- `history.*` - история измерений
- `settings.*` - настройки
- `stats.*` - статистика
- `permission.*` - разрешения
- `error.*` - ошибки
- `onboarding.*` - онбординг
- `paywall.*` - экран подписки

---

## 🌍 Добавление нового языка

### Шаг 1: Обновить Project Settings
1. Откройте проект в Xcode
2. Выберите Project Navigator (слева)
3. Выберите проект "Sound Level Meter"
4. Вкладка "Info" → "Localizations"
5. Нажмите `+` и выберите язык

### Шаг 2: Добавить переводы
1. Откройте `Localizable.xcstrings`
2. В правой панели появится новый язык
3. Переведите все строки

### Шаг 3: Тестирование
```swift
// В Settings app → General → Language & Region
// Смените язык и проверьте приложение
```

---

## 🔄 Workflow для переводчиков

### Для переводчика БЕЗ доступа к Xcode:

1. Откройте `Localizable.xcstrings` в текстовом редакторе
2. Найдите секцию вашего языка (например, `"es"`)
3. Переведите значение в поле `"value"`
4. Установите `"state": "translated"`

Пример:
```json
"history.title" : {
  "localizations" : {
    "es" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Historial"  ← Перевести это
      }
    }
  }
}
```

### Для переводчика С доступом к Xcode:

1. Откройте `Localizable.xcstrings` в Xcode
2. Используйте встроенный редактор
3. Xcode покажет статус переводов (✓/⚠️)
4. Export/Import XLIFF для профессиональных CAT-tools

---

## 🎨 Специальные случаи

### Плюрализация (plural forms)
```json
"items.count" : {
  "localizations" : {
    "en" : {
      "variations" : {
        "plural" : {
          "one" : {
            "stringUnit" : {
              "value" : "%d item"
            }
          },
          "other" : {
            "stringUnit" : {
              "value" : "%d items"
            }
          }
        }
      }
    }
  }
}
```

### Форматирование с параметрами
```swift
String(localized: "greeting.hello",
       defaultValue: "Hello, \(name)!",
       comment: "Greeting with name")
```

### Device-specific строки
```json
"device.type" : {
  "localizations" : {
    "en" : {
      "variations" : {
        "device" : {
          "ipad" : "iPad",
          "iphone" : "iPhone"
        }
      }
    }
  }
}
```

---

## ✅ Checklist перед релизом

- [ ] Все строки имеют переводы на 7 языков
- [ ] Нет ключей со статусом `"needs_review"`
- [ ] Протестировано на всех языках
- [ ] Длинные тексты не обрезаются в UI
- [ ] Числа, даты форматируются корректно
- [ ] RTL языки (если добавятся) отображаются правильно

---

## 🆘 Troubleshooting

### Перевод не отображается
1. Проверьте, что ключ правильно написан
2. Clean Build Folder (Cmd+Shift+K)
3. Перезапустите приложение

### Новый язык не появляется
1. Убедитесь, что язык добавлен в Project Settings
2. Проверьте Info.plist → CFBundleLocalizations
3. Пересоберите проект

### Как экспортировать для переводчиков
```bash
# В Xcode:
# Editor → Export For Localization...
# Получите XLIFF файлы для перевода
```

---

## 📚 Дополнительные ресурсы

- [Apple Localization Guide](https://developer.apple.com/localization/)
- [String Catalogs Documentation](https://developer.apple.com/documentation/xcode/localizing-and-varying-text-with-a-string-catalog)
- [WWDC23: Discover String Catalogs](https://developer.apple.com/videos/play/wwdc2023/10155/)

---

**Последнее обновление:** 2026-01-12
**Версия:** 1.0
**Статус:** 7 языков полностью поддерживаются
